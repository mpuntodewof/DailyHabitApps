# syntax=docker/dockerfile:1.7
# Development image — Vite with hot reload. Bind-mount the source tree from the host.
FROM node:20-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
